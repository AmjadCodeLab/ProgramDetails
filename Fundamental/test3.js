input = "2[ab]"
outut = abab

input = 3[b2[ca]]
3b 2ca
bcaca 1
bcaca 2
bcaca 3
output = bcacabcacabcaca

let str = input.replace(/'[]'/,'')split(''); //  ["3","b","2","c","a"]; 
   str.reduce(function(ele,index)=>{
      if (ele[index].chatAt(index) != NaN) {
        object[result]++
        console.log(object[result])   // bbb
      }
      else if (obj[result] == NaN)
}
 

xy cordinates